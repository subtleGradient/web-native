# base-separator

`base-separator` is an accessible separator custom element ported from Base UI Separator behavior.

```html
<base-separator></base-separator>
<base-separator orientation="vertical"></base-separator>
```

The host exposes `role="separator"`, `aria-orientation`, and `data-orientation`.
